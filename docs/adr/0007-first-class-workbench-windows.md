---
status: accepted
---

# First-class Workbench Windows

Pictor models multiple equally capable native Workbench Windows, each with stable identity, independent View composition and focus, and its own Project or Session selection. The first delivery may omit cross-window dragging, grouping, navigation, animation, and per-window Runtime ownership, but the Workbench model must not assume one native window or distinguish primary and secondary windows.
